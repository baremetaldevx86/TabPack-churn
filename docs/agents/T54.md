# T54 — Recovery and reconciliation receipt

## Scope and completion count

Read `swarm.md` first and audited every original `docs/agents/T1.md` through
`T50.md` path. **42 original notes existed; 8 were absent: T1, T5, T7, T10,
T25, T30, T34 and T40.** Recovered notes were created under their original
filenames and each explicitly says **“recovered by T54 after interrupted
callback”**. The 42 existing notes were not overwritten. All original rows T1–T50
are now `done`; this is coordinator-authorized ledger reconciliation and does
not claim an interrupted callback successfully returned.

**Original assignment status: 50/50 task notes complete; 42 found + 8
recovered; 0 original rows remain running/blocked.** No T1–T50 research/design
agent created production source or executable test files. Eleven assignments
recorded bounded read-only probes, arithmetic/oracle checks or Git observations;
the remaining notes accurately state that project tests, installs, training or
downloads were not run. Later implementation receipts are separate: T51 reports
78 focused model/optimizer tests (including 2 CUDA), T52 reports 70 passed and 9
skipped data tests, and T53 reports 140 core tests passed. T54 did not rerun
these commands.

## Evidence decisions and disagreements reconciled

1. **Reference identity.** The official code SHA
   `05a89e21b955f12de84889d662e15ca534019aaa` is a valid 40-character GitHub
   commit SHA; GitHub's commit API and tree endpoint confirm it. It is the
   upstream reference identity, never the local implementation SHA. Local Git
   is unborn and must remain `null`/`unborn_repository` until a coordinator
   checkpoint exists (T39/T48).
2. **Metric.** Pinned Churn config/report uses task **accuracy** as `score`;
   upstream main report shows validation accuracy `.87625` vs ROC-AUC
   `.8643926187747397`, and test accuracy `.857` vs ROC-AUC `.8585596382206551`
   (T1/T3/T6). The reduced local contract deliberately selects/checkpoints by
   validation ROC-AUC and reports local test ROC-AUC/log-loss. These numbers must
   never be compared as if they shared a metric. Local tie-aware grouped AUC
   fixes the pinned Torch shortcut's within-score tie issue identified in T3;
   T53's 8,604-vector oracle receipt supports that local contract.
3. **Selection.** T16 proposed an empty 0.5 baseline, canonical-key sorting and
   warm starts. Decision 9 and T3/T23/T53 supersede that proposal: best singleton
   always, supplied stable pool order, strict additions and strict outer
   incumbent retention, rebuilding from the caller's prior-snapshot-plus-current
   pool. Snapshot identity is candidate ID/epoch; repeated member IDs at distinct
   epochs remain legal.
4. **Protocol labels.** “Optimistic” means repeating the full candidate
   sampling/training/selection workflow per seed (paper §4.2); it does not mean
   test leakage. Separately, validation checkpoint/greedy reuse is
   selection-optimistic (T4/T47). The test split must remain untouched until
   selection freezes.
5. **Data trust boundary.** T18/T38 discussed partial extraction and separately
   pinned member hashes; T52 resolved this in code by retaining the **complete**
   archive, verifying its exact size/SHA-256 into a private snapshot, deriving
   member hashes only from those verified bytes, and comparing local extracted
   bytes. This is full-archive verification plus Churn-only publication, not a
   range/prefix verification and not an editable self-certifying receipt (T7/T52).
6. **Sampling/RNG.** T13/T26 left per-run versus shared sampling open; T34
   recovery and T53 implementation evidence resolve the current policy as
   per-run heterogeneous redraw with SHA-256 named streams, independent member
   initialization/permutation/dropout identities and no Python `hash()`/additive
   seeds. Actual trainer checkpoint/RNG continuation remains unverified.
7. **Fairness.** T40/T4/T46 reconcile that equal epochs and per-member batch do
   not equalize parameters, FLOPs, padding, validation search or wall time.
   The three-way experiment is a reduced workflow comparison; a packing-only
   causal or speed claim needs a matched looped-vs-packed ablation.

## Source, license and dataset-card reconciliation

The pinned HF dataset revision is
`dfb85cb493bfb7493de0b8c0bd9e2f9e6a56baa1`; provider metadata publishes archive
size `189856645` and LFS SHA-256
`89338c628fed24af03084c9348ca0a5c8ca4f12f8b988a576d9d1711cc661558`. No local
archive was downloaded by T54, so this is provider-published expected identity,
not an observed local digest. The actual Churn member inventory, feature names,
train class counts, semantic target meaning and original license remain
unobserved/unconfirmed. The official README says it adds no restrictions beyond
the original dataset licenses; the HF repository exposes no dataset license in
the inspected metadata. T10/T30 correctly leave legal clearance open rather
than inventing Kaggle/IBM terms.

Paper Table 5 establishes Churn Modelling as 6,400/1,600/2,000 with 7 numeric,
3 binary and 1 categorical features. T8's “11 categorical” wording is a rendered
HTML transcription error and was corrected in recovered T1/T30; it must not be
used as an archive-member claim. The local source lock in `docs/data-source.json`
and T52 implementation intentionally fail closed rather than substitute IBM
Telco or synthetic data.

## Remaining blockers

- Acquire and parse the complete pinned archive; retain actual member inventory,
  schema, source target semantics, and observed hashes.
- Resolve or explicitly document the original dataset license/redistribution
  terms before a release claiming legal data provenance.
- Integrate the training/selection/CLI runner, checkpoint/RNG chronology and
  final aggregation; no end-to-end Churn measurements exist in the recovered
  notes.
- Recheck the isolated dependency environment (T51 recorded a later torch-import
  stall; T52 skipped sklearn tests and had no Ruff) before release gates.
- Preserve the user's unresolved CPU-friendly three-seed/four-member versus
  larger GPU budget decision from the ledger; do not silently retune after test
  inspection.

## Checks performed by T54

Read all 42 existing notes, the eight recovered-note source/evidence sets,
current `tabpack/core.py`, `data.py`, `models.py`, `docs/data-source.json`, T51–T53
receipts, pinned GitHub API/source/config/report, arXiv v1 HTML, and Hugging Face
revision/tree metadata. No source, tests, README, Git history, install,
acquisition, official execution or training was performed by T54.
